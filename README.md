# RandomizedLoadBalancing
Monte Carlo simulation of randomized load balancing. N servers each have a load in  0 , 1 0,1. For each new task, we sample 1..K random servers and assign it to the least loaded one. Over T trials, we estimate the expected load of the chosen server for each K.
